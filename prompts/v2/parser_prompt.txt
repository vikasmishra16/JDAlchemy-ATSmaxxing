TODO: Convert the following resume text into structured JSON matching the Resume schema.

Return only valid JSON. No markdown or explanations.
Extract only facts explicitly supported by the resume text. Leave unsupported fields empty.

Resume text:
{raw_text}
